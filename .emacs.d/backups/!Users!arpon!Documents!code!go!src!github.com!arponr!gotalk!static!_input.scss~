.message-input {
    position: absolute;
    bottom: 0;
    width: 100%;

    .message-area, .message-options, .preview-pane {
        position: absolute;
        border-top: 1px solid $border-light;
        width: 100%;
    }

    .message-area, .preview-pane {
        bottom: 30px;
        padding-top: 5px;
        padding-bottom: 5px;
        font-family: $serif;
    }

    .preview-pane {
        overflow: auto;
    }

    .down {
        position: absolute;
        bottom: 140px;
        left: -30px;
        padding: 10px;

        font-size: 40px;

        display: none;
    }

    .message-options {
        height: 30px;
        bottom: 0;

        font-family: $sans;
        font-size: $size-small;

        color: $color-light;

        .send-button, .preview-button {
            padding: 0 15px;
            height: 100%;
            float: left;

            &:hover {
                color: $color-base;
                background: $light-highlight;
            }

            &:active {
                @include inset-shadow(6px, rgba(0,0,0,0.1));
            }
        }
    }

    .check {
        position: relative;
        margin: 7px 15px 0 0;
        float: right;

        cursor: pointer;

        input {
            visibility: hidden;
        }

        &:hover {
            color: $color-base;
        }

        &:hover .checklabel:before,
        input:checked + .checklabel:before {
            border-color: $border-dark;
        }

        input:checked + .checklabel:after {
            content: "\2713";
            position: absolute;
            left: -2px;
        }
    }

    .checklabel {
        @extend .noselect;

        &:before {
            content: "";
            position: absolute;
            left: -3px;
            bottom: 1px;
            padding: 5px;

            border: 1px solid $border-light;
        }
    }
}