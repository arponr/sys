$login-width: 32rem;
$login-input-width: 70%;

.login {
    height: 100%;
    color: $grey-dark;
    background: $grey-lighter;

    .content {
        position: absolute;
        top: 50%;
        left: 50%;
        width: $login-width;
        margin: -12rem 0 0 (-$login-width)/2;
        text-align: center;
    }

    header {
        h1 {
            font-size: 2.4rem;
            font-weight: 300;
            margin-bottom: 0.5rem;
        }

        h2 {
            font-size: 1.2rem;
            line-height: 1.1;
        }

        &:after {
            content: '';
            display: block;
            width: $login-width/3;
            margin: 3rem auto;
            height: 1px;
            background: $grey-light;
        }
    }

    input[type="text"], input[type="password"] {
        width: $login-input-width;
        margin-bottom: 1.5rem;
    }

    .info {
        color: $warning;
    }

    .submit, .toggle {
        margin-top: 0.5rem;
    }

    .submit {
        width: 4/10 * $login-input-width;
        margin-right: calc(1/10 * #{$login-input-width} - 4px);
    }

    .toggle {
        width: 5/10 * $login-input-width;
    }
}