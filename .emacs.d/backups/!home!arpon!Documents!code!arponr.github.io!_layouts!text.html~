{% include header.html %}

    <header>
      {% if page.title %}<h1>{{ page.title }}</h1>{% endif %}
      {% if page.date %}<h2>{{ page.date | date_to_string }}</h2>{% endif %}
      {% if page.preview %}<h3>{{ page.preview }}</h3>{% endif %}
    </header>

    {% if page.banner_url %}
    <figure class="wrap">
      <img src="/images/{{ page.banner_url }}" alt="{{ page.banner_alt }}">
      <figcaption>
        {{ page.banner_cap }}
      </figcaption>
    </figure>
    {% endif %}

    <article class="wrap text">
      {{ content }}
    </article>

{% include footer.html %}
