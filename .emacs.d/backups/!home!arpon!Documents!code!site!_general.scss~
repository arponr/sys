* {
    margin: 0;
    padding: 0;
    -moz-box-sizing: border-box;

    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;

    &:focus {
        outline: none;
    }
}

html {
    font-family: $serif;
    font-size: 90%;
    color: $color-base;
}

a {
    border-bottom: 1pt solid $link-border;

    &:link, &:visited {
        text-decoration: none;
        color: $color-base;
    }

    &:hover {
        color: $color-base;
    }
}

p {
    margin-bottom: 10px;
}

ul {
    list-style: none;
}

.wrapper {
    width: 500px;
    margin: 60px auto;
    position: relative;
}

.head {
    margin-bottom: 40px;

    h1, h2 {
        font-weight: normal;
    }

    h1 {
        font-size: $size-big;
    }

    h2 {
        font-size: $size-base;
        color: $color-light;
    }
}

.text {
    line-height: 1.2em;
    text-align: justify;
}