.page {
    height: 100%;
    width: 100%;
}

$toolbar-height: 40px;
$toolbar-pad: 15px;

.toolbar {
    width: 100%;
    height: $toolbar-height;
    padding: 0 $toolbar-pad / 4;
    color: $grey-light;
    border-bottom: 1px solid $grey-light;

    .icon {
        padding: 0 $toolbar-pad / 2;
        font-family: $font-icon;
        font-size: $toolbar-height;
        cursor: pointer;

        &:hover {
            color: $grey-mediumer;
        }
    }
}

.loc {
    position: absolute;
    top: $toolbar-height;
    bottom: 0;

    &.left {
        left: 0;
        width: 320px;
    }

    &.right {
        right: 0;
        width: 280px;
    }
}

$view-vpad: 8px;
$view-hpad: 10px;

.view {
    width: 100%;
    height: 100%;

    .left &, .right & {
        color: $grey-dark;
        background: $grey-lighter;

        .item {
            width: 100%;
            padding: $view-vpad $view-hpad;
        }

        .item-abs {
            position: absolute;
            left: $view-hpad;
            right: $view-hpad;
        }

        header {
            h1 {
                font-size: $size-big;
                letter-spacing: -0.04rem;
                color: $grey-mediumer;
            }
        }
    }

    .left & {
        border-right: 1px solid $grey-light;
    }

    .right & {
        border-left: 1px solid $grey-light;
    }
}