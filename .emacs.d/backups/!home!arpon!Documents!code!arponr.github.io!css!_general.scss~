$serif: "calluna", serif;
$sans: "freight-sans-pro", sans-serif;

$size-base: 1rem;
$size-small: .9rem;
$size-big: 1.1rem;
$size-huge: 1.5rem;

$color-base: #303540;
$color-light: #707580;
$color-link: #000510;

$border-light: #c0c5d0;
$border-dark: #707580;

$width: 450px;
$nav-height: 20px;
$stream-width: $width * 2/3;
$stream-margin: 15px;
$stream-date-width: 80px;

* {
    margin: 0;
    padding: 0;

    -webkit-box-sizing: border-box;
    -moz-box-sizing: border-box;
    box-sizing: border-box;

    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;

    &:focus {
        outline: none;
    }
}

html {
    font-family: $serif;
    font-size: 90%;
    color: $color-base;
}

body {
    overflow-y: scroll;
}

a:link, a:visited {
    text-decoration: none;
    color: $color-base;
}

p, blockquote {
    margin-bottom: 10px;
}

blockquote {
    font-family: $sans;
    color: $color-light;
    text-align: right;
}

ul {
    list-style: none;
}

h1, h2, h3 {
    font-weight: normal;
    font-size: $size-base;
}

figure {
    img {
        max-width: 100%;
    }

    figcaption {
        font-family: $sans;
        font-size: $size-small;
        color: $color-light;
        text-align: center;
        margin-top: 5px;
    }
}

.clear {
    clear: both;
}

.wrap {
    position: relative;
    width: $width;
    margin: 30px auto;
}

nav {
    height: $nav-height;
    border-bottom: 1px solid $border-light;
    font-family: $sans;
    font-size: $size-small;
    color: $color-light;
    text-transform: uppercase;

    li {
        display: inline;
        float: right;
        margin-left: 15px;
        height: $nav-height;

        a:link, a:visited {
            color: $color-light;
        }

        &.active {
            border-bottom: 1pt solid $border-dark;

            a:link, a:visited {
                color: $color-base;
            }
        }
    }
}

footer {
    font-family: $sans;
    font-size: $size-small;
    text-align: right;

    a:link, a:visited {
        text-decoration: underline;
    }
}

header {
    h1 {
        font-size: $size-huge;
    }

    h2, h3 {
        margin-top: 2px;
        font-family: $sans;
        color: $color-light;
    }

    h3 {
    }
}

article h1 {
    font-size: $size-small;
    text-transform: uppercase;
    margin-bottom: 15px;
}

.text {
    line-height: 1.35em;
    text-align: justify;

    a:link, a:visited {
        text-decoration: underline;
        color: $color-link;
    }

    ul, ol {
        margin: 10px 0 10px 35px;
    }

    ul li {
        margin-bottom: 5px;
        text-indent: -13px;

        &:before {
            content: "\2014";
            position: relative;
            left: -10px;
        }
    }
}

.stream li {
    margin-bottom: $stream-margin;
    width: $stream-width;

    .desc, .date {
        font-family: $sans;
    }

    .date {
        color: $color-light;
        font-size: $size-small;
        width: $stream-date-width;
        float: left;
    }

    .title {
        display: block;
        margin-left: $stream-date-width;
    }

    .desc {
        display: none;
        position: absolute;
        left: $stream-width + $stream-margin;
        width: $width - $stream-width - $stream-margin;
    }
}
