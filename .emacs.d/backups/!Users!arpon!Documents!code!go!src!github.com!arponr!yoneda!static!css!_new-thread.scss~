.new-thread {
    .inputs {
        top: $size-big + 3.5 * $line-base + 1px;
    }

    .name, .users {
        width: 100%;
        @include rounded($grey-light);
        padding: 5px;
    }

    .name {
        margin-bottom: $line-base;
    }

    /* .message-input { */
    /*     top: (5 * $view-vpad) + ($size-big * $line-base-unitless) + (2 * $input-height); */

    /*     .area, .live { */
    /*         top: 0; */
    /*     } */

    /*     .live { */
    /*         bottom: calc(50% + #{$message-options-height} / 2 + #{$line-base} / 2); */
    /*     } */

    /*     .area { */
    /*         bottom: $message-options-height; */
    /*         transition: top 0.15s; */

    /*         &.half { */
    /*             top: calc(50% - #{$message-options-height} / 2 + #{$line-base} / 2); */
    /*         } */
    /*     } */
    /* } */
}
